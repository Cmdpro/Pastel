package earth.terrarium.pastel.data.lang.guidebook.pastelnetwork;

import net.neoforged.neoforge.common.data.LanguageProvider;

public class ConnectionNodeEntry {
	public static void addTranslations(LanguageProvider provider) {
		provider.add("book.pastel.guidebook.connection_node.name", "Connection Node");
		provider.add("book.pastel.guidebook.connection_node.page0.text", "As the simplest of nodes the Connection Node has no active use.\\\n\\\nThey solely act as a cheap and unobtrusive way of extending the reach of my networks.");
		provider.add("book.pastel.guidebook.connection_node.page1.text", "Like all other nodes, the Connection Node has a range of 12 blocks.");
	}
}
