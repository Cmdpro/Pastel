package earth.terrarium.pastel.data.lang.guidebook.deco;

import net.neoforged.neoforge.common.data.LanguageProvider;

public class GlowblocksEntry {
	public static void addTranslations(LanguageProvider provider) {
		provider.add("book.pastel.guidebook.glowblocks.name", "Glowblocks");
		provider.add("book.pastel.guidebook.glowblocks.page0.text", "I now have pigments as pure as can be. So why not make blocks with color as pure as can be!\\\n\\\nI thought about calling them [#](449900)Antiblocks[#](), but since they are indeed blocks that would be silly, wouldn't it?");
		provider.add("book.pastel.guidebook.glowblocks.page1.text", "Glowblocks glow from the inside so they always look the same, no matter how much light falls on their surface.");
	}
}
