package earth.terrarium.pastel.data.lang.guidebook.magicalblocks;

import net.neoforged.neoforge.common.data.LanguageProvider;

public class BlockPlacerEntry {
	public static void addTranslations(LanguageProvider provider) {
		provider.add("book.pastel.guidebook.block_placer.page0.text", "Automatically breaking blocks, weighing them, sorting them or putting them in one of my chests is easy. But there is one thing that all my previous machines could never do: place things. That is now a thing of the past.\\\n[Topaz](entry://general/shards@topaz), with its affinity for matter, allows it to materialize blocks from its inventory.");
		provider.add("book.pastel.guidebook.block_placer.page1.text", "*It does the thing!*");
	}
}
